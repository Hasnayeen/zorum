Quick starting point for rapid app development

- Add `.env` file and set variables

- Rename certificates files in `docker/certs` folder

- Update host name in `docker/site.dev.conf` file

```bash
$ ./install.sh  
```

  * Laravel
  * Livewire
  * TailwindCSS